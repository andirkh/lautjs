// The app's client entry -- the one script the sea loads. Anything app-specific
// (locale, theme, analytics) runs here, before Laut's runtime takes over.
import { start } from "lautjs/client";

start();
